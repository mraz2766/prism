# Prism 项目协作规则

本文件补充全局 Agent 规则，适用于 Prism 项目及其全部子目录。

## 修改后的固定交付流程

每次完成一批用户可见的代码、资源或文档修改后，只提升一次应用版本，并在交付前完成以下步骤：

1. 默认按“小改动”处理；只有用户明确说明是大改动，或改动明显改变产品核心能力/架构时，才按“大改动”处理并记录决定。
2. 小改动执行 `./scripts/release.sh small`：次版本加 `0.1`，例如 `1.0.0 → 1.1.0`。
3. 大改动执行 `./scripts/release.sh major`：主版本加 `1`、次版本归零，例如 `1.2.0 → 2.0.0`。
4. 发布脚本必须通过测试和 Release 构建后，才可替换 `/Applications/Prism.app`。
5. 最终回复必须报告应用版本、内部构建号、测试结果和安装路径。

计划文件的创建或审核阶段不提升版本、不构建、不安装。实现失败或验证失败时，不得覆盖当前已安装的 App。

## 版本事实来源

应用版本只以 `Prism.xcodeproj/project.pbxproj` 中的以下 Xcode build settings 为准：

- `MARKETING_VERSION`：用户可见版本，格式为 `主.次.0`
- `CURRENT_PROJECT_VERSION`：内部构建号，每次成功交付递增 `1`

不要创建额外版本文件，也不要引入 Git 提交或标签工作流。

